import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';
import path from 'path';

export default defineConfig({
    plugins: [
        laravel({
            input: ['resources/css/app.css', 'resources/js/app.js'],
            refresh: true,
        }),
    ],
    build: {
        manifest: true, // This is required to generate manifest.json
        outDir: path.resolve(__dirname, 'public/build'), // Ensure this points to the correct output directory
    },
});
